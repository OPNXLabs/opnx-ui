<UserControl x:Class="OPNX.UI.WPF.Controls.OpnxDateRangeSelector"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:OPNX.UI.WPF.Controls"
             mc:Ignorable="d"
             d:DesignHeight="36"
             d:DesignWidth="620"
             x:Name="Root">
    <UserControl.Resources>
        <Style x:Key="RangeRadioButtonStyle"
               TargetType="{x:Type RadioButton}">
            <Setter Property="MinWidth" Value="30" />
            <Setter Property="Height" Value="28" />
            <Setter Property="Margin" Value="0,0,0,0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Foreground" Value="#8FA3B8" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontWeight" Value="Normal" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type RadioButton}">
                        <Border x:Name="border"
                                MinWidth="{TemplateBinding MinWidth}"
                                Height="{TemplateBinding Height}"
                                Padding="{TemplateBinding Padding}"
                                Background="#0C1724"
                                BorderBrush="Transparent"
                                BorderThickness="0"
                                CornerRadius="{Binding CornerRadius}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              RecognizesAccessKey="True" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.7" />
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#4A6FE3" />                                
                                <Setter Property="Foreground" Value="#FFFFFFFF" />                                
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RangeDatePickerStyle"
               TargetType="{x:Type local:OpnxDatePicker}">
            <Setter Property="Width" Value="126" />
            <Setter Property="Height" Value="28" />            
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Foreground" Value="#8FA3B8" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="IconBrush" Value="#FF8D8D8D" />            
            <Setter Property="IsHitTestVisible" Value="False" />            
            <Setter Property="Background">
                <Setter.Value>
                    <SolidColorBrush Color="#8FA3B8" Opacity="0.1"></SolidColorBrush>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <DataTrigger Binding="{Binding ElementName=Root, Path=IsCustomRange}" Value="True">
                    <Setter Property="IsHitTestVisible" Value="True" />
                    <Setter Property="Opacity" Value="1" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition Height="10"></RowDefinition>
            <RowDefinition Height="30"></RowDefinition>
        </Grid.RowDefinitions>

        <ItemsControl Grid.Row="0"
                      ItemsSource="{Binding ElementName=Root, Path=Items}" VerticalAlignment="Center" HorizontalAlignment="Stretch">            
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Rows="1" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate DataType="{x:Type local:OpnxDateRangeItem}">
                    <RadioButton Content="{Binding Header}"
                                 Margin="{Binding ItemMargin}"
                                 GroupName="{Binding RadioGroupName, RelativeSource={RelativeSource AncestorType={x:Type local:OpnxDateRangeSelector}}}"
                                 Tag="{Binding RangeType}"
                                 IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                 Checked="RangeRadioButton_Checked"
                                 Style="{StaticResource RangeRadioButtonStyle}" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <local:OpnxDatePicker Grid.Column="0" SelectedDate="{Binding ElementName=Root, Path=FromDateTime, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                  DisplayFormat="{Binding ElementName=Root, Path=DateDisplayFormat}"
                                  Style="{StaticResource RangeDatePickerStyle}" />

            <TextBlock Grid.Column="1"
                       Width="12"
                       Foreground="#FF707070"
                       FontSize="12" 
                       Margin="7.5,0,7.5,0"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       TextAlignment="Center"
                       Text="-" />

            <local:OpnxDatePicker Grid.Column="2" SelectedDate="{Binding ElementName=Root, Path=ToDateTime, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                  DisplayFormat="{Binding ElementName=Root, Path=DateDisplayFormat}"
                                  Style="{StaticResource RangeDatePickerStyle}" />
        </Grid>
    </Grid>
</UserControl>
